@page
@model RadioAmateurHelper.Pages.ServicesModel
@{
    ViewData["Title"] = "Услуги и помощь по ремонту";
}
<style>
    .header-overlay {
        background: rgba(255, 255, 255, 0.6);
        backdrop-filter: blur(5px);
        -webkit-backdrop-filter: blur(5px);
        padding: 50px 20px;
        border-radius: 12px;
        /* Плавная равномерная прозрачность со всех сторон */
        mask-image: linear-gradient(to top, rgba(0,0,0,0) 0%, rgba(0,0,0,1) 10%, rgba(0,0,0,1) 90%, rgba(0,0,0,0) 100%), linear-gradient(to right, rgba(0,0,0,0) 0%, rgba(0,0,0,1) 3%, rgba(0,0,0,1) 97%, rgba(0,0,0,0) 100%);
        -webkit-mask-image: linear-gradient(to top, rgba(0,0,0,0) 0%, rgba(0,0,0,1) 10%, rgba(0,0,0,1) 90%, rgba(0,0,0,0) 100%), linear-gradient(to right, rgba(0,0,0,0) 0%, rgba(0,0,0,1) 3%, rgba(0,0,0,1) 97%, rgba(0,0,0,0) 100%);
        mask-composite: intersect;
        -webkit-mask-composite: destination-in;
    }
</style>
<div class="header-overlay">
<h2>Услуги и помощь по ремонту</h2>
<p>Мы предлагаем следующие услуги:</p>
<ul>
    <li>Ремонт радиотехники</li>
    <li>Прошивка микроконтроллеров</li>
    <li>Сборка и настройка схем</li>
    <li>Подбор компонентов</li>
    <li>Онлайн-помощь</li>
</ul>
<h3>Оставить заявку</h3>
<form method="post">
    <label asp-for="UserName">Имя:</label>
    <input asp-for="UserName" class="form-control" />

    <label asp-for="Contact">Контакт:</label>
    <input asp-for="Contact" class="form-control" />

    <label asp-for="Message">Сообщение:</label>
    <textarea asp-for="Message" class="form-control"></textarea>

    <button type="submit" class="btn btn-success mt-2">Отправить</button>
</form>
@if (Model.SuccessMessage != null)
{
    <div class="alert alert-success mt-3">
        @Model.SuccessMessage
    </div>
}
</div>